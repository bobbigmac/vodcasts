Ingest existing show filter files to determine the types of shows we want, check the cache for feeds lacking show filter files, and based on actual feed content, thoughtfully identify and write show filters for more channels—recognizing title patterns and grouping episodes into meaningful shows. There is a helper script in /scripts that generates a temporary report listing feeds missing show filters, as well as feeds with filters and their detected shows, to support review. Continue this process in batches, reporting how many feeds have well-crafted, content-informed show filters versus those that do not, ensuring each channel has carefully considered and appropriately filtered shows based on actual content analysis rather than generic scripting.